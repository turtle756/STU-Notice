# 서울신학대학교 총학생회 웹사이트

## 파일 구조

```
docs/
├── index.html              ← 메인 페이지 (일정) - GitHub Pages 필수
│
├── common/                 ← 수정 불필요
│   ├── style.css           ← 전체 스타일
│   ├── script.js           ← 전체 기능
│   ├── events.html         ← 행사/공모전 페이지
│   ├── clubs.html          ← 동아리/소모임 페이지
│   └── partners.html       ← 제휴사 페이지
│
├── edit/                   ← 정기 수정 파일
│   ├── schedule.js         ← 캘린더 이미지 경로
│   ├── events.js           ← 행사/공모전 카드 데이터
│   ├── clubs.js            ← 동아리/소모임 카드 데이터
│   └── partners.js         ← 제휴사 카드 데이터 + 지도
│
└── image/                  ← 이미지 저장 폴더
    └── calendar/           ← 월별 캘린더 이미지
```

## 카드 수정 방법

모든 데이터 파일은 동일한 구분선 형식 사용:

```javascript
// @@@@@@@@@@@@@@@@@
{
  // 카드 내용
}
// #####################

,

// @@@@@@@@@@@@@@@@@
{
  // 다음 카드
}
// #####################
```

- **카드 추가**: `@@@@` ~ `####` 블록 복사 후 붙여넣기
- **카드 삭제**: `@@@@` ~ `####` 블록 통째로 삭제
- **쉼표(,)**: 블록 사이에 위치 (삭제 시 영향 없음)

## 제휴사 지도 추가

카카오맵 "지도 퍼가기" 코드를 그대로 복붙:

```javascript
// --------------------------
// 모달용 지도 (560x300)
// --------------------------
mapCodeModal: `여기에 카카오맵 코드 붙여넣기`
// --------------------------
```

## 실행

```bash
npx http-server docs -p 5000 --cors -c-1
```

## GitHub Pages

Settings → Pages → `/docs` 선택
